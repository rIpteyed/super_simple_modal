<html>
    <head>
        <title>HTML and CSS</title>
        <style>
            .modal {
                font-family: Verdana, Times;
                background-color: lightblue;
                color: yellow;
                border: 1px solid grey;
                position: relative;
                width: 40vw;
                height: 20vh;
                margin: 30px auto 0 auto;
                border-radius: 5px;
                box-shadow: 2px 2px 5px grey;
            }

            .open {
                visibility: visible;
                transition: opacity .5s linear;
                opacity: 1;
            }

            .closed {
                visibility: hidden;
                transition: visibility 0s .5s, opacity .5s linear;
                opacity: 0;
            }

            .header {
                position: relative;
                font-size: 1.3em;
                padding: 13px 0 0 10px;
                border-bottom: 1px solid grey;
                height: 40px;
                background-color: grey;
                opacity: 1;
                color: white;
                font-weight: 700;
            }

            .body {
                padding: 20px;
            }

            .footer {
                position: absolute;
                height: 40px;
                bottom: 0px;
                width: 100%;
                border-top: 1px solid grey;
            }

            .footer button {
                position: relative;
                float: right;
                margin: 10px 5px 5px 0;
                height: 20px;
            }
	    
            button {
                position: relative;
                margin: 10px auto 0 auto;
            }
        </style>
        <script>
            function openModal(element, id) {
                element.className = "closed";
                document.getElementById(id).className = "modal open";
            }

            function closeModal(element, buttonId) {
                element.parentNode.parentNode.className = 'modal closed';
                document.getElementById(buttonId).className = "open";
            }
        </script>
    </head>

    <body>
        <button class="open" id="openButton" onClick="openModal(this, 'myModal')">open</button>
        <div class="modal closed" id="myModal">
            <div class="header">Modal Title</div>
            <div class="body">The quick brown fox jumped over the lazy dog.</div>
            <div class="footer">
                <button onClick="closeModal(this, 'openButton')">Close</button>
            </div>
        </div>
    </body>
</html>
